name: Command via SSH

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: ls -a via OPEN SSH Private Key
        uses: garygrossgarten/github-action-ssh@release
        with:
          command: ls -a
          host: http://ci-cd.bubeph.com/
          username: garygrossgarten
          password: locka
          privateKey: bomo
        env:
          CI: true